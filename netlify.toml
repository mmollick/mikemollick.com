[build]
  command = "git config url.\"https://${GITHUB_TOKEN}@github.com/\".insteadOf \"https://github.com/\" && git submodule update --init && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "24"
  GIT_SUBMODULE_STRATEGY = "none"
